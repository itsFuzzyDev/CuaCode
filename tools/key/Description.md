---
name: key
output:
  pressed: str
active: True
---
Presses a key or key combination, e.g. "cmd+c", "ctrl+alt+delete",
"escape", "return". Modifiers: cmd/command, ctrl/control, alt/option,
shift. Use lowercase, join with "+". For plain text input use
type_text instead.